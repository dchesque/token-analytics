# ==============================================
# CORE DEPENDENCIES
# ==============================================

# HTTP requests and data manipulation
requests>=2.31.0
pandas>=2.0.0
python-dotenv>=1.0.0

# Console output and cross-platform compatibility
rich>=13.5.0
colorama>=0.4.6

# ==============================================
# WEB APPLICATION DEPENDENCIES
# ==============================================

# Flask web framework and components
Flask>=2.3.0
Flask-CORS>=4.0.0
gunicorn>=21.0.0

# Flask dependencies (usually installed automatically)
Jinja2>=3.1.0
MarkupSafe>=2.1.0
Werkzeug>=2.3.0
click>=8.1.0
itsdangerous>=2.1.0

# ==============================================
# AI INTEGRATION DEPENDENCIES
# ==============================================

# Type hints and HTTP clients for AI APIs
typing-extensions>=4.8.0
httpx>=0.25.0
aiohttp>=3.9.0

# JSON validation and error handling
jsonschema>=4.20.0
tenacity>=8.2.0

# Caching and rate limiting
cachetools>=5.3.0
ratelimiter>=1.2.0

# ==============================================
# HYBRID AI MODE DEPENDENCIES (OPTIONAL)
# ==============================================

# Web scraping and HTML parsing
beautifulsoup4>=4.12.0
lxml>=4.9.0
html5lib>=1.1

# RSS feed parsing and sentiment analysis
feedparser>=6.0.0
textblob>=0.17.0

# Premium search API clients (optional)
tavily-python>=0.3.0

# Text processing and validation
nltk>=3.8.0
fake-useragent>=1.4.0
validators>=0.22.0
python-slugify>=8.0.0

# Advanced caching and HTTP session management
requests-cache>=1.1.0
python-dateutil>=2.8.0
diskcache>=5.6.0

# ==============================================
# INSTALLATION NOTES
# ==============================================
# 
# CORE MODE (basic functionality):
#   pip install -r requirements.txt --no-deps && pip install requests pandas rich python-dotenv colorama Flask Flask-CORS gunicorn
#
# FULL MODE (with AI integration):
#   pip install -r requirements.txt
#
# DEVELOPMENT MODE (all dependencies):
#   pip install -r requirements.txt
#   python -c "import nltk; nltk.download('vader_lexicon')"  # For sentiment analysis
#
# Docker build should now complete without errors!